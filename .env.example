POSTGRES_USER=energymanager
POSTGRES_PASSWORD=changeme
POSTGRES_DB=energymanager

# Used by the api service when running via docker compose or locally
DATABASE_URL=postgres://energymanager:changeme@localhost:5432/energymanager

API_PORT=3000
WEB_PORT=8080
